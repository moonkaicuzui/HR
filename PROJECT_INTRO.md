# HR Dashboard System - 프로젝트 소개
# HR Dashboard System - Project Introduction

> 이 문서는 AI 어시스턴트(Claude 등)에게 프로젝트를 소개하기 위해 작성되었습니다.
> This document is written to introduce the project to AI assistants (Claude, etc.).

---

## 1. 프로젝트 개요 / Project Overview

### 무엇을 하는 시스템인가? / What does this system do?

HR Dashboard System은 **HR 데이터를 분석하여 자체 포함형(self-contained) HTML 대시보드를 생성**하는 Python 기반 시스템입니다.

- **입력**: CSV/Excel 형식의 HR 데이터 (인력 현황, 출근 기록, 성과 데이터 등)
- **출력**: 외부 의존성 없이 브라우저에서 바로 열 수 있는 단일 HTML 파일
- **특징**: 3개 언어 지원 (한국어/영어/베트남어), 런타임 언어 전환 가능

### 핵심 설계 철학 / Core Design Philosophy

```
┌─────────────────────────────────────────────────────────────┐
│  "설정이 코드를 지배한다" / "Configuration Rules Over Code"  │
│                                                             │
│  비즈니스 로직 = JSON 설정 파일                               │
│  Python 코드 = 범용 실행 엔진                                 │
└─────────────────────────────────────────────────────────────┘
```

**3대 원칙:**

1. **Configuration Over Code**: 모든 비즈니스 규칙은 JSON 설정 파일에 정의
2. **Subject/Metric Agnostic**: 동일한 함수가 모든 주제(팀/직급/전체)와 메트릭 조합에서 작동
3. **NO FAKE DATA**: 데이터가 없으면 빈 결과 반환, 절대로 가짜 데이터 생성 금지

---

## 2. 디렉토리 구조 / Directory Structure

```
HR/
├── config/                      # 📁 설정 파일 (비즈니스 로직)
│   ├── metric_definitions.json  # 메트릭 계산 공식, 임계값
│   ├── chart_templates.json     # 차트 템플릿 설정
│   ├── dashboard_config.json    # 대시보드 레이아웃
│   ├── translations.json        # 다국어 번역 (KO/EN/VI)
│   ├── design_tokens.json       # 디자인 시스템 토큰
│   └── drive_config.json        # Google Drive 연동 설정
│
├── src/                         # 📁 소스 코드
│   ├── visualization/           # 시각화 모듈
│   │   ├── complete_dashboard_builder.py  # ⭐ 메인 대시보드 빌더
│   │   ├── style_generator.py   # CSS 생성기
│   │   ├── js_utilities.py      # JavaScript 유틸리티
│   │   └── virtual_scroll.py    # 가상 스크롤링 (대용량 테이블)
│   │
│   ├── analytics/               # 분석 모듈
│   │   ├── metric_calculator.py # 메트릭 계산 엔진
│   │   └── hr_metric_calculator.py
│   │
│   ├── core/                    # 핵심 모듈
│   │   ├── data_loader.py       # 데이터 로더
│   │   ├── data_validator.py    # 데이터 검증기
│   │   └── error_detector.py    # 6종 에러 감지
│   │
│   ├── integration/             # 통합 모듈
│   │   └── google_drive_sync.py # Google Drive 동기화
│   │
│   ├── utils/                   # 유틸리티
│   │   ├── i18n.py              # 다국어 지원
│   │   ├── date_parser.py       # 12+ 날짜 형식 파싱
│   │   └── logger.py            # 로깅
│   │
│   └── generate_dashboard.py    # 대시보드 생성 진입점
│
├── tests/                       # 📁 테스트
│   └── test_visualization_modules.py  # 37개 단위 테스트
│
├── input_files/                 # 📁 입력 데이터 (CSV/Excel)
├── output_files/                # 📁 출력 (생성된 HTML)
├── docs/                        # 📁 배포된 대시보드
│
├── action.sh                    # 🚀 실행 스크립트 (인터랙티브)
├── CLAUDE.md                    # Claude Code 가이드
├── ARCHITECTURE.md              # 상세 아키텍처 문서
└── requirements.txt             # Python 의존성
```

---

## 3. 핵심 모듈 설명 / Key Modules

### 3.1 complete_dashboard_builder.py (메인 빌더)

**역할**: 모든 컴포넌트를 조합하여 최종 HTML 대시보드 생성

**생성하는 대시보드 구성:**
- **Tab 1 - Executive Summary**: 11개 KPI 카드 + 인터랙티브 모달
- **Tab 2 - Trends**: 다중 월 트렌드 차트 (Chart.js)
- **Tab 3 - Employee Details**: 직원 테이블 (필터/검색/정렬/페이지네이션)
- **Tab 4 - Help**: 사용 가이드

**기술적 특징:**
- 자체 포함형 HTML (CSS/JS 모두 임베디드)
- XSS 방지 보안 처리
- 접근성 준수 (WCAG 2.1 AA)
- 반응형 디자인 (모바일 대응)
- 가상 스크롤링 (1000+ 행 성능 최적화)

### 3.2 metric_calculator.py (메트릭 계산 엔진)

**역할**: JSON 설정 기반 메트릭 계산

```python
# metric_definitions.json에서 공식 로드
{
  "absence_rate": {
    "formula": "(absent_days / total_days) * 100",
    "thresholds": {
      "excellent": {"max": 2, "color": "#10b981"},
      "good": {"max": 5, "color": "#3b82f6"},
      "warning": {"max": 10, "color": "#f59e0b"},
      "critical": {"min": 10, "color": "#ef4444"}
    }
  }
}
```

**특징**: 새 메트릭 추가 시 JSON만 수정, Python 코드 수정 불필요

### 3.3 data_loader.py (데이터 로더)

**역할**: 다중 소스 데이터 로딩 + 캐싱

**지원 데이터 소스:**
- Basic Manpower Data (기본 인력 데이터)
- Attendance Records (출근 기록)
- AQL History (품질 이력)
- 5PRS Data (성과 데이터)
- Team Structure (팀 구조)

**NO FAKE DATA 정책 구현:**
```python
try:
    df = pd.read_csv(file_path)
except FileNotFoundError:
    return pd.DataFrame()  # 빈 DataFrame 반환, 가짜 데이터 생성 안함
```

### 3.4 error_detector.py (에러 감지기)

**6종 에러 카테고리:**
1. **Temporal Errors**: 미래 날짜, 잘못된 시퀀스
2. **TYPE Errors**: 누락/잘못된 분류
3. **Position Errors**: 누락된 직급
4. **Team Errors**: 알 수 없는 팀 참조
5. **Attendance Errors**: 실제 > 총일수, 음수 값
6. **Duplicate Errors**: 중복 사번

---

## 4. 실행 방법 / How to Run

### 인터랙티브 모드 (권장)
```bash
./action.sh
```

### 직접 실행
```bash
python src/generate_dashboard.py --month 12 --year 2025 --language ko
```

### 출력 위치
```
output_files/HR_Dashboard_Complete_2025_12.html
```

---

## 5. 설정 파일 가이드 / Configuration Guide

### 새 메트릭 추가 (코드 수정 없이)

**Step 1**: `config/metric_definitions.json` 수정
```json
{
  "new_metric": {
    "id": "new_metric",
    "formula": "YOUR_FORMULA",
    "data_sources": ["attendance"],
    "thresholds": {
      "excellent": {"max": 5, "color": "#28a745"}
    }
  }
}
```

**Step 2**: `config/translations.json` 수정
```json
{
  "ko": {"metrics": {"new_metric": "새 메트릭"}},
  "en": {"metrics": {"new_metric": "New Metric"}},
  "vi": {"metrics": {"new_metric": "Chỉ số mới"}}
}
```

**Step 3**: `./action.sh` 실행 - 완료!

---

## 6. 코딩 규칙 / Coding Guidelines

### 이중 언어 주석 (필수)
```python
def calculate_metric(self, data: pd.DataFrame):
    """
    Calculate metric value from data
    데이터로부터 메트릭 값 계산
    """
    # Initialize result container
    # 결과 컨테이너 초기화
    result = {}
```

### 절대 금지 사항
- ❌ 비즈니스 로직을 Python에 하드코딩
- ❌ 가짜/합성 데이터 생성
- ❌ 주제별 중복 함수 작성 (parameterize 사용)
- ❌ 영어 또는 한국어 단일 주석

### 권장 사항
- ✅ JSON 설정으로 비즈니스 로직 분리
- ✅ 매개변수화된 범용 함수 사용
- ✅ 이중 언어(한/영) 주석 작성
- ✅ 데이터 없으면 빈 결과 반환

---

## 7. 최근 개선 사항 / Recent Improvements

### 디자인 시스템 모듈화
- `design_tokens.json`: 중앙 집중식 디자인 변수
- `style_generator.py`: 토큰 기반 CSS 생성
- `js_utilities.py`: 모듈화된 JS 유틸리티 (Security, A11y, Performance)

### 성능 최적화
- `virtual_scroll.py`: 1000+ 행 테이블 가상 스크롤링
- 로딩 인디케이터 추가
- 필터/검색 상태 localStorage 저장

### 접근성 개선
- 터치 타겟 최소 44x44px
- ARIA 속성 강화
- 에러 경계 (Error Boundary) 추가
- 검색 하이라이팅

### 테스트
- 37개 단위 테스트 (모든 모듈 커버)
- 5개 사용자 페르소나 관점 테스트

---

## 8. 아키텍처 다이어그램 / Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                         action.sh                                │
│                    (Interactive Entry Point)                     │
└──────────────────────────┬──────────────────────────────────────┘
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│                   generate_dashboard.py                          │
│                    (Orchestrator)                                │
└──────────────────────────┬──────────────────────────────────────┘
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│              complete_dashboard_builder.py                       │
│                    (Main Builder)                                │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐             │
│  │   Charts    │  │   Tables    │  │   Modals    │             │
│  └─────────────┘  └─────────────┘  └─────────────┘             │
└──────────────────────────┬──────────────────────────────────────┘
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│                      Analytics Layer                             │
│  ┌──────────────────┐  ┌──────────────────┐                     │
│  │ MetricCalculator │  │  ErrorDetector   │                     │
│  │  (JSON-driven)   │  │  (6 categories)  │                     │
│  └──────────────────┘  └──────────────────┘                     │
└──────────────────────────┬──────────────────────────────────────┘
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│                        Core Layer                                │
│  ┌────────────┐  ┌────────────┐  ┌────────────┐                 │
│  │ DataLoader │  │    I18n    │  │ DateParser │                 │
│  │ (Caching)  │  │  (KO/EN/VI)│  │(12+ formats)│                │
│  └────────────┘  └────────────┘  └────────────┘                 │
└──────────────────────────┬──────────────────────────────────────┘
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│                   Configuration Layer                            │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐ │
│  │metric_definitions│ │ chart_templates │  │  translations   │ │
│  │     .json       │  │     .json       │  │     .json       │ │
│  └─────────────────┘  └─────────────────┘  └─────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│                        Output                                    │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │  HR_Dashboard_Complete_2025_12.html                         ││
│  │  (Self-contained: CSS + JS + Data embedded)                 ││
│  └─────────────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────────────┘
```

---

## 9. 기술 스택 / Tech Stack

### Backend
- **Python 3.8+**
- **pandas**: 데이터 처리
- **openpyxl**: Excel 파일 처리

### Frontend (생성된 HTML 내)
- **Bootstrap 5**: UI 프레임워크
- **Chart.js 4**: 차트 라이브러리
- **Vanilla JavaScript**: 인터랙션

### Optional
- **Google API**: Google Drive 동기화

---

## 10. 문의 시 알아야 할 것 / What to Know When Asking

이 프로젝트에 대해 질문할 때 다음을 기억하세요:

1. **새 기능 추가**: 먼저 JSON 설정으로 해결 가능한지 확인
2. **메트릭 변경**: `metric_definitions.json` 수정 권장
3. **UI 변경**: `design_tokens.json` 또는 `dashboard_config.json` 확인
4. **다국어 추가**: `translations.json`에 번역 추가
5. **코드 수정 시**: 이중 언어 주석 필수

---

## 요약 / Summary

| 항목 | 설명 |
|------|------|
| **목적** | HR 데이터 → 자체 포함형 HTML 대시보드 생성 |
| **핵심 원칙** | Configuration Over Code, NO FAKE DATA |
| **언어 지원** | 한국어, 영어, 베트남어 (런타임 전환) |
| **주요 파일** | `complete_dashboard_builder.py`, `metric_definitions.json` |
| **실행 방법** | `./action.sh` 또는 `python src/generate_dashboard.py` |
| **출력** | 단일 HTML 파일 (CSS/JS/데이터 모두 임베디드) |

---

*이 문서는 2025년 12월 기준으로 작성되었습니다.*
*This document was written as of December 2025.*
